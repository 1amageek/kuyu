import UniformTypeIdentifiers

public extension UTType {
    static let kuyuProject = UTType(exportedAs: "team.stamp.kuyu-project", conformingTo: .package)
    static let manasBundle = UTType(exportedAs: "team.stamp.manas-bundle", conformingTo: .package)
}
